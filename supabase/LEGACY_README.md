# Legacy — Supabase reference only

This folder is kept for historical reference. It is **not used** by this
version of the project, which runs on Cloudflare Workers + D1 instead of
Supabase.

The active database schema is in `../cloudflare/migrations/0001_schema.sql`.
The active backend API is in `../cloudflare/worker/index.js`.

If you're setting this project up fresh, you can ignore everything in this
folder.
